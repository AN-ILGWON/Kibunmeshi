<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kibunmeshi.repository.FoodCategoryMapper">

    <resultMap id="FoodCategoryResultMap" type="com.kibunmeshi.domain.FoodCategory">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="label" column="label"/>
        <result property="icon" column="icon"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="FoodCategoryResultMap">
        SELECT id, name, label, icon, created_at
        FROM food_categories
        ORDER BY id ASC
    </select>

    <select id="findById" resultMap="FoodCategoryResultMap">
        SELECT id, name, label, icon, created_at
        FROM food_categories
        WHERE id = #{id}
    </select>

</mapper>
